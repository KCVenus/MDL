{% extends 'base.html.twig' %}

{% block hero %}

        <div class=''> 
            <img class='relative w-screen h-screen z-1' src='https://plus.unsplash.com/premium_photo-1661876708169-5656991eb206?q=80&w=2012&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D' alt='hero' />
        </div>
        <div class='relative top-72 flex items-center justify-center '>
            <div class='z-10 absolute'><h1 class='text-white text-6xl'>MAISON DES LIGUES</h1> <div class='flex items-center justify-center'><span class='text-white text-center mt-4 text-2xl'>Arthur Demoisson & Fabio Borner</span></div>  </div></div>
  <p>Testttttttttttt</p>
  
     <div class="container-lg bg-warning bg-opacity-10">
        {% for atelier in ateliers %}
            {% if loop.index0 % 3 == 0 %}
                <div class="row">
                {% endif %}
                <div class="col-md-4 border border-warning">
                    <div class="item">
                        <h5>{{ atelier.libelle }} :</h5>
                        <p>Nombres de places : {{ atelier.nbplacesmaxi }}</p>
                        <p>Les thèmes : </p>
                        <p>
                            {% foreach theme in themes %}
                                {% if atelier.id  ==  theme.id %}
                                    -{{ theme.libelle }}<br>
                                {% endif %}
                            {% endfor %}
                        </p>
                    </div>
                </div>
                {% if loop.index0 % 3 == 2 or loop.last %}
                </div>
            {% endif %}
        {% endfor %}
    </div>
{% endblock %}

